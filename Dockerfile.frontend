FROM nginx:alpine

# Copy built assets from local machine (pre-built)
COPY web-dashboard/dist /usr/share/nginx/html

# Copy custom nginx config
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
